# Parity Review

## Status

The original Vite/React Forkcast site has been restored at the repository root. The Astro rebuild now lives in `forkcast-astro/`, and the benchmark/comparison presentation app lives in `forkcast-bench/`.

The route-family parity work, quality gates, benchmark pass, representative visual review, and completion audit remain preserved with the Astro project. The Astro project is no longer promoted to the repository root; it is maintained side by side with the original app so both implementations can be compared directly.

## Confirmed

- The greenfield project uses Astro 6, React integration, Tailwind, shadcn/ui, lucide icons, pnpm, and Biome.
- `pnpm verify` passes with zero Astro check errors, zero Astro check warnings, zero Biome errors, and zero Biome warnings.
- The production build emits 973 static pages.
- The Astro project files and migration artifacts are preserved under `forkcast-astro/`.
- The original Vite/React project is restored at the repository root.
- `forkcast-bench/` renders the benchmark/comparison deck from the recorded route-level legacy and Astro benchmark JSON files.
- Baseline and Astro benchmark artifacts are recorded:
  - `migration/baseline-benchmark.json`
  - `migration/baseline-benchmark.md`
  - `migration/astro-benchmark.json`
  - `migration/astro-benchmark.md`
- The refreshed Astro benchmark hit 973 routes with 0 HTTP route failures, 7,590 total requests, 769,078.9 KB transferred, 24,983.4 KB JavaScript transferred, and 0 app-owned data/API fetches.
- The average cold route is 57.6% smaller by transferred bytes and 98.1% smaller by JavaScript than the legacy Vite/React baseline. Raw HTML usefulness increased from 161 characters per route to 27,712 characters per route.
- `astro/public/llms.txt` now describes the static Astro site instead of the old SPA.
- The shared Astro shell now matches the legacy void theme more closely: the dark slate palette override, Forkcast wordmark asset, upgrades chevron, filled theme toggle, and announcement external-link icon are restored.
- Main route families render HTML: home, upgrades, EIPs, calls, devnets, decisions, agenda, schedule, rank, and legacy aliases.
- Browser smoke checks passed for the primary route families and for EIP search, EIP analysis/spec tabs, call filtering, and call-page search.
- The `/calls` route now includes a static upcoming-call section generated from the GitHub PM snapshot, with agenda/live links, call-series badges, and search/filter participation.
- Browser smoke checks passed for `/calls`: upcoming ACDT #79 and ACDC #178 rows render, upcoming live-stream labels render, search for `glamsterdam devnet updates` narrows to the ACDT upcoming row, the AllCoreDevs filter hides FCR rows, and zero browser warnings/errors were reported.
- The `/schedule` route now mounts the legacy-style ACD planning sandbox with editable dates, locked dates, custom phase durations, devnet count controls, localStorage persistence, and Gantt-style planning. It also prerenders a useful default projection table from the schedule domain model as the no-JS/crawler fallback instead of a loading message.
- Browser smoke checks passed for `/schedule`: page title and heading, static fallback content present in built HTML, settings/reset controls, devnet add/remove controls, editable date activation, devnet count mutation, and zero console warnings/errors.
- Schedule date/progress calculation and known-milestone projection merging now live under `astro/src/domain/schedule`; the React planner consumes `buildSchedulePlan` instead of rebuilding Glamsterdam/Hegota progress in the component.
- Browser smoke checks passed after the schedule-domain cleanup: page title and heading, one reset control, settings expansion with eight duration inputs, editable date activation, and zero console warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/schedule` is recorded in `migration/screenshots/legacy-schedule-1280x720.png` and `migration/screenshots/astro-schedule-1280x720.png`. The planner table/workspace and shared shell have close desktop parity after restoring the legacy dark palette, wordmark logo, upgrades chevron, filled theme toggle, and announcement external-link icon.
- The `/rank` route now renders the Hegota headliner tier-maker with S/A/B/C/D rows, EL/CL grouped proposal collections, drag-and-drop ranking, click/keyboard/touch assignment fallback, localStorage-backed ranking state, reset/download controls, proposal tooltips, and the same Hegota proposal set including the pending PeerDAS item.
- Browser smoke checks passed for `/rank`: page title and heading, instruction expansion, grouped proposal rendering, click assignment into S tier, keyboard selection assignment into A tier, local reset cleanup, download-button invocation after a ranked state, and zero console warnings/errors.
- Native Playwright Chromium validation passed for `/rank`: dragging `eip-7807` into S tier created a ranked row, clicking Download Image emitted `hegota-headliner-rankings.png`, and the saved artifact at `migration/downloads/hegota-headliner-rankings.png` has a valid PNG signature with no browser warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/rank` is recorded in `migration/screenshots/legacy-rank-1280x720.png` and `migration/screenshots/astro-rank-1280x720.png`. That pass caught and fixed a tier-row fill regression so the S/A/B/C/D rows now render as flush colored bands like the legacy tier board.
- The Glamsterdam `/upgrade/glamsterdam/client-priority` tab now renders static aggregate client-priority data with sortable proposal rows, EL/CL client stance badges, active-only filtering, filter modal controls, detail expansion, source links, and score legend.
- Browser smoke checks passed for `/upgrade/glamsterdam/client-priority`: page title and headings, score sorting with no-data rows after scored rows, filter modal open/apply/close, filtered count updates, detail expansion with client stance/source rows, and zero console warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/upgrade/glamsterdam/client-priority` is recorded in `migration/screenshots/legacy-client-priority-1280x720.png` and `migration/screenshots/astro-client-priority-1280x720.png`. That pass restored the legacy upgrade shell, full Glamsterdam description, meta-discussion link treatment, underlined section tabs, active Client Priority tab, compact toolbar, and static client-priority table density.
- Browser smoke checks passed after the client-priority visual parity pass: the page title and upgrade shell render, the active tab is visibly selected, the filter modal opens, the execution-layer filter applies, the filtered badge updates, a responsive priority row expands to reveal client source links, active-only remains checked, and zero browser warnings/errors were reported.
- The Glamsterdam `/upgrade/glamsterdam/test-complexity` tab now uses a static STEEL complexity snapshot generated by `pnpm data:complexity`, with sortable proposal rows, active-only filtering, tier filter modal controls, detail expansion, source links, anchor score details, and score legend.
- Browser smoke checks passed for `/upgrade/glamsterdam/test-complexity`: page title and headings, score sorting, tier filter open/apply/close, filtered count updates, detail expansion with anchor scores/full assessment link, and zero console warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/upgrade/glamsterdam/test-complexity` is recorded in `migration/screenshots/legacy-test-complexity-1280x720.png` and `migration/screenshots/astro-test-complexity-1280x720.png`. That pass restored the legacy upgrade shell, tab underline treatment, STEEL summary copy, compact toolbar, and first-table viewport while keeping STEEL assessment data static instead of reintroducing the legacy runtime refresh.
- Browser smoke checks passed after the test-complexity visual parity pass: the active tab is visibly selected, the tier filter modal opens, the High tier filter applies, the filtered badge updates, an assessed row expands to show anchor details/source links, and zero browser warnings/errors were reported.
- EthPandaOps active-network metadata is now captured by `pnpm data:devnets` into a static Astro domain snapshot, avoiding the legacy runtime networks fetch for Glamsterdam devnet inclusion.
- GitHub PM upcoming-call issue/comment metadata is now captured by `pnpm data:upcoming-calls` into a static Astro domain snapshot, avoiding the legacy runtime GitHub fetch on agenda, calls, and EIP pages.
- The Glamsterdam `/upgrade/glamsterdam/devnet-inclusion` tab now joins proposal metadata, active devnet inclusion, STEEL testing complexity, and client priority into one static domain model with active-only filtering, hide-in-devnet filtering, stage/layer filter modal controls, sortable columns, active-devnet badges, complexity links, support scores, and signal legend.
- Browser smoke checks passed for `/upgrade/glamsterdam/devnet-inclusion`: page title, active devnet rows without inactive historical devnets, stage/layer filtering, hide-in-devnet count updates, complexity sorting, and zero console warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/upgrade/glamsterdam/devnet-inclusion` is recorded in `migration/screenshots/legacy-devnet-inclusion-1280x720.png` and `migration/screenshots/astro-devnet-inclusion-1280x720.png`. That pass restored the legacy upgrade shell, active Devnet Inclusion tab, single-line explanatory copy, compact filter toolbar, and table entry point while retaining static snapshot metadata for assistive/no-JS contexts.
- Browser smoke checks passed after the devnet-inclusion visual parity pass: the active tab is visibly selected, active-only and hide-in-devnet controls render, the filter modal opens, the execution-layer filter applies, the filtered badge updates, static EIP rows and complexity source links remain visible, and zero browser warnings/errors were reported.
- The `/agenda` route now renders agenda suggestions, linkable series URL state, next scheduled GitHub call cards, pending upgrade scope, EIP discussion-thread excerpts, open action items, resolved action rollups, deferred decisions, revisited decisions, recent call outcomes, artifact generated dates, and call/EIP links from a typed static `agenda` domain model.
- Browser smoke checks passed for `/agenda`: page title and heading, ACDE planning counts, suggested agenda content, next scheduled ACDT #79 and ACDC #178 cards, upcoming agenda/live links, upgrade scope, open actions, deferred decisions, recent calls, call links, `/agenda?series=acdc` filtering, All-series reset, and zero browser warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/agenda` is recorded in `migration/screenshots/legacy-agenda-1280x720.png` and `migration/screenshots/astro-agenda-1280x720.png`. That pass restored the legacy ACDE default, max-width rhythm, underlined series dropdown, compact suggested-agenda card header, priority dots, and non-tabbed page chrome.
- Browser smoke checks passed after the agenda visual parity pass: `/agenda` defaults to the ACDE-only view, the series dropdown opens, All shows all three series with section headings, ACDC filters to the consensus series, and zero browser warnings/errors were reported.
- Call detail pages now parse transcripts and chat logs into typed timestamped entries, apply artifact sync offsets, filter pre-call lead-in content, render summary highlights plus key decisions/action items/targets with timestamp links, and support static deep-link highlighting for transcript hashes like `#t=00:03:24` and chat links like `?chat=00:04:02`.
- Browser smoke checks passed for `/calls/acde/236`: synced first transcript timestamp `00:03:24`, synced first chat timestamp `00:04:02`, transcript deep-link highlighting, chat deep-link highlighting, summary decision/action/target sections, and zero browser warnings/errors.
- Call detail pages now provide the legacy-style current-call search modal from static rendered HTML. The header search control and Cmd/Ctrl+K open the modal, result selection deep-links to transcript or chat timestamps, and no old 13 MB global search corpus is fetched on page load.
- Browser smoke checks passed for `/calls/acde/236`: header search opens the modal, `Svalbard` finds transcript results from rendered HTML, transcript/chat deep links highlight the synced rows, and zero browser warnings/errors were reported.
- Call detail pages now render the TLDR summary as an expandable static summary panel with highlight/action/decision counts. The panel is collapsed by default to match the legacy workspace while keeping the summary content in the prerendered HTML.
- Call detail timestamp links now use the synced display timestamp consistently across transcript rows and TLDR summary links, update the hash, highlight the matching transcript row, and send `seekTo`/`playVideo` commands to the active YouTube iframe through the iframe API.
- Native Playwright Chromium validation passed for `/calls/acde/236`: clicking the first transcript timestamp emitted YouTube `seekTo(204, true)` and `playVideo`, clicking the first summary timestamp used the synced `00:03:24` link and highlighted the matching transcript row, and zero app warnings/errors were reported with the YouTube iframe stubbed.
- Native Playwright Chromium validation passed for call theater mode: at 1280x720 the desktop theater toggle switched labels between `Theater Mode` and `Exit Theater`, kept the page width bounded, and constrained transcript/chat cards to 384px scroll panels; at 390x844 the theater toggle remains hidden and there is no horizontal overflow. The old iframe `web-share` feature warning was removed.
- ACDT call detail pages now render BAL/ePBS breakout views from static breakout artifacts, including breakout video embeds, query-param view switching, parsed breakout chats, and chat timestamp highlighting.
- Browser smoke checks passed for `/calls/acdt/078`: default main-call view, BAL and ePBS query switching, parsed breakout chat rows, `?breakout=...&chat=...` deep-link highlighting, and zero browser warnings/errors.
- A 1280x720 side-by-side screenshot pass for `/calls/acde/236` is recorded in `migration/screenshots/legacy-call-acde-236-1280x720.png` and `migration/screenshots/astro-call-acde-236-1280x720.png`. That pass restored the legacy call workspace entry point: no extra page header, video workspace on the left, split Transcript/Chat Logs rail on the right, title/date/agenda metadata inside the video card, synced transcript/chat timestamps, and compact card typography.
- EIP detail pages now restore static EIP search modal integration, spec-content search through a compact term index, Cmd/Ctrl+K opening, ArrowLeft/ArrowRight previous/next navigation, discussion/spec header links, copyable author handles with readable author metadata, latest breakout-call links for mapped proposals, static upcoming-call link rendering when the snapshot has a mapped breakout series, and North Star alignment sections.
- Browser smoke checks passed for `/eips/7732`: one search control, seven author-handle copy controls, latest ePBS call link, clean author text extraction, modal search returning EIP-7732, Cmd/Meta+K opening, ArrowRight navigation to `/eips/7736`, and zero browser warnings/errors. `/eips/7904` also renders the North Star alignment section with zero browser warnings/errors.
- Browser smoke checks passed for EIP spec search: `trusted payments promises` loads the static spec term index on demand, ranks EIP-7732 first, marks it as `matched spec`, and reports zero browser warnings/errors. The generated payload is 1.1 MB raw and about 291 KB gzip, instead of loading full markdown text.
- A 1280x720 side-by-side screenshot pass for `/eips/7732` is recorded in `migration/screenshots/legacy-eip-7732-1280x720.png` and `migration/screenshots/astro-eip-7732-1280x720.png`. That pass restored the legacy EIP detail entry point: top search pill, card-local discussion/spec links, inline copyable author handles without a visible handles row, Glamsterdam-first timeline ordering, headliner presentation history, champion metadata, and compact card/timeline density.
- Browser smoke checks passed after the EIP detail visual pass: the search modal opens and returns EIP-7732, seven inline author-handle copy controls remain, the timeline starts with Glamsterdam and includes scheduled headliner state plus ACDC #158 presentation history, analysis/spec tabs switch, ArrowRight navigates to `/eips/7736` from body focus, the mobile viewport has no horizontal overflow, and zero browser warnings/errors were reported.
- The final route-family visual pass covered home, upgrades index, Glamsterdam overview, Hegota overview, EIPs index, calls index, devnets index, `/devnets/glamsterdam`, decisions, and feedback redirect behavior. That pass restored the devnet series model and aliases, legacy upgrade overview shell, compact calls calendar, plain decisions feed, and auto-redirecting feedback page.
- The 2026-05-10 side-by-side Chrome review covered `/`, `/upgrades`, `/upgrade/glamsterdam`, `/upgrade/hegota`, `/calls`, `/calls/acde/236`, `/eips`, `/eips/7732`, `/decisions`, `/schedule`, and `/agenda` against the restored root app. The focused follow-up fixed the Glamsterdam overview sidebar to use legacy-style status groups and restored the `/upgrades` Live table so Dencun, Shapella, The Merge, and previous-upgrade references are inline like the original page.
- The 2026-05-10 Chrome sweep also opened `forkcast-bench/` and confirmed the benchmark deck renders P95 metrics, the route-by-route table, and the headline transfer/JavaScript reductions without browser errors.
- Devnet routes now have a clearer domain boundary through `astro/src/domain/devnets/series.ts`, which models user-facing series, spec-backed routes, series aliases, and active network-only devnet routes in one place instead of duplicating category and route concepts across pages.

## Parity Gaps

- No known route-family functional parity gaps remain from the current review.
- The current snapshot has no mapped upcoming ePBS/BAL/FOCIL issue, so the EIP-page upcoming-call path still needs a future-data smoke test when matching upstream data exists.
- Live/external data sources were removed for performance, with static replacement snapshots now covering GitHub PM upcoming-call issue/comment context, STEEL test-complexity assessments, and EthPandaOps active-network metadata.
- The side-by-side visual review is representative across route families, not an exhaustive screenshot comparison of all 973 generated routes.

## Current Structure

- `./` is the original Vite/React Forkcast app.
- `./forkcast-astro/` is the Astro 6 migration with close visual and functional parity.
- `./forkcast-bench/` is the benchmark and adoption-review presentation app.

The Astro project should only be promoted again if the team decides to adopt it after reviewing the side-by-side product parity and the benchmark deck.
